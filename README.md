# Portfolio CV UI

Frontend-only copy of the public portfolio CV UI.

## Run

```powershell
npm install
npm start
```

If you use PowerShell and `npm` is blocked by execution policy, use:

```powershell
npm.cmd install
npm.cmd start
```

The app runs without the backend. All content is stored in `src/data/portfolioData.js`.
